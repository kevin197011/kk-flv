services:
  backend:
    build: ./backend
    expose:
      - "8080"
    environment:
      - ADDR=:8080

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      - API_PROXY_TARGET=http://backend:8080
    depends_on:
      - backend
